poll_interval: 3000
slack:
  channel: "my-test-channel"
  token: "xoxb-todo"
db: "./db.sqlite"
certs_dir: "./certs"
server_addr: "127.0.0.1:8080"
auth:
  admin: ["hsg", "ssg"]
  guest: ["ncar", "root"]
cluster:
  shell:
    siblings_cmd: "todo"
    cousins_cmd: "todo"
    real_node_cmd: "todo"
scheduler:
  shell: 
    status_cmd: "todo"
    release_cmd: "todo"
    offline_cmd: "todo"
